# Optional. If unset, the app uses http://localhost:8080 (local Spring Boot).
# Set this when building or running against a deployed API (no trailing slash required).
#
# Copy to .env.development.local to override locally, or set in Netlify/Vercel
# "Environment variables" for production builds.
#
# REACT_APP_API_BASE_URL=https://your-api.example.com
